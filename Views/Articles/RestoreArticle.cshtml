@using PBL3.Models
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Khôi phục bài viết";
}
@model Article
<div class="addEditPage">
    <div class="grid">
        <div class="grid_row">
            <div class="grid_column">
                <div class="addEditPage_header">
                    <h1>Khôi phục bài viết</h1>
                </div>
                <div class="addEditPage_input">
                    <div asp-validation-summary="ModelOnly" class="div-danger"></div>
                    <form asp-action = "RestoreArticle">
                        @await Html.PartialAsync("_Article", Model)
                        <div class="formSubmitGroup" style="position: sticky; bottom: 0; background-color: #fff">
                            <div class="formSubmit">
                                <button type="submit" class="submitButton button">
                                    <i class="fas fa-window-restore"></i>
                                    Khôi phục
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>